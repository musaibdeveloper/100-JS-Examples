//03. JavaScript Program to Calculate the Area of a Triangle.

// To calculate a area of traingle we know the formula.

// A = 1/2 * b *h

let base = +prompt("Enter the base : ");

let height = +prompt("Enter the height : ");

let cal = 1/2*base*height

console.log(`The area of traingle is ${cal}`);